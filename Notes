-fix alignment of game recap screen
-adjust look of session stats screen

-fix bug on level 29 (piece randomly changes - could be reset function delay)

-build proper music menu eventually

-code refactor!!!
>apply array method with other counters or stats
>add notation or tidy up any current notation if needed;
>player class still needs a bit of work

-add comments to CSS to break up sections (or create separate pages);

-add next block toggle 'on' or 'off'